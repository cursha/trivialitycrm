# Module Six Delivery Report — Phases A–E (complete)

## Starting point

Modules One through Five (CRM foundation, AI-assisted lead discovery, production hardening, Sales Workspace, Reporting and Analytics — including the scheduled-report worker) were complete, tested, and merged into `main` before this module started. Module Six Phase A was built on branch `module-six-communications`, following a 12-section plan approved before any code was written (provider comparison, database design, permission matrix, consent design, phased implementation order, and test plan — see the plan's phasing in "Deviations" below for how it maps to this report). Each phase since has followed the same pattern Module Five established: build on a fresh branch off the updated `main`, ship a tested checkpoint, merge, repeat — rather than one long-lived branch for the whole module. Phase A merged first (`module-six-communications`), then Phase B (`module-six-consent-compliance`), then Phase C (`module-six-scheduling-sequences`), then a post-Phase-C fixes checkpoint (`module-six-phase-c-fixes`), then Phase D1 (`module-six-calendar`), then Phase D2 (`module-six-inbound-sync`), then **Phase E on `module-six-phase-e`**.

Phase D itself (calendar/inbound sync in the original plan) was split into two checkpoints at the user's request — D1 (calendar/appointments) then D2 (inbound sync) — the same reasoning Module Five split into core reporting then scheduled reports, and Module Six's own Phase A/B/C split: smaller, independently-shippable checkpoints rather than one large sweep.

**This report covers Phases A through E — the full approved plan.** Every phase in the original 12-section plan (mailbox connections, email templates, the composer/send action, consent/compliance, scheduled sends, multi-step follow-up sequences, calendar/appointment scheduling, inbound email sync, and now bounce detection/unified notifications) is built, tested, and merged. Module Six is feature-complete against its approved scope; see "Remaining limitations / known gaps" for what's deliberately out of scope (bulk send, a multi-contact composer, Gmail inbound sync) rather than unfinished.

## Provider architecture

A single internal `EmailProvider` interface (`src/lib/comms/providers/types.ts`) with three implementations, mirroring the existing AI-research provider pattern (`src/lib/research/providers/`) exactly — a `factory.ts` selects an implementation behind a shared interface, already proven replaceable in this codebase:

| | Microsoft Graph | Google (Gmail) | Mock |
|---|---|---|---|
| **Auth** | OAuth 2.0 authorization-code, `login.microsoftonline.com` | OAuth 2.0 authorization-code, `accounts.google.com`, `access_type=offline&prompt=consent` for a reliable refresh token | None — no network |
| **Send** | Create-draft-then-send (`POST /me/messages` then `POST /me/messages/{id}/send`) — Graph's direct `sendMail` returns 202 with no message id, so this is the only path that yields a `providerMessageId` to store | Hand-built RFC 2822 MIME message, base64url-encoded (Gmail's own non-standard alphabet), `POST /gmail/v1/users/me/messages/send` | Deterministic fake ids (`mock-msg-<uuid>`), never a real-looking one |
| **Active when** | A user connects Microsoft | A user connects Google | Always, unconditionally, when `NODE_ENV=test` — enforced in `factory.ts`, not left to whichever provider a `ProviderConnection` row happens to specify |

Provider selection is **per-connection**, not a global env var — unlike AI research's single `AI_PROVIDER`, a user can only meaningfully connect to whichever provider their employer's tenant uses, so `ProviderConnection.provider` is the selector and `getEmailProvider(kind)` takes it as a parameter.

## Features shipped (Phase A)

- **Mailbox connections** (`/settings/email-connections`, `connect_mailbox`): OAuth connect/disconnect, one connection per user (`ProviderConnection.userId` unique). CSRF-protected via a short-lived, httpOnly `oauth_state_<provider>` cookie compared at the callback. Access/refresh tokens are encrypted at rest (AES-256-GCM, `src/lib/comms/token-crypto.ts`, `TOKEN_ENCRYPTION_KEY`) and only ever decrypted server-side; the UI only ever sees connection status (connected/expired/revoked/error), never a token. `getUsableAccessToken()` transparently refreshes a token within 5 minutes of expiry and marks the connection `ERROR` (with `lastError`) on a failed refresh rather than throwing an opaque error.
- **Email templates** (`/settings/email-templates`, `manage_personal_templates`/`manage_shared_templates`): personal (owner-only) or shared (team-wide) templates with name/category/subject/body/active/leadType/pipelineStage/language. Placeholders (`{{contact.firstName}}`, `{{contact.lastName}}`, `{{contact.email}}`, `{{company.name}}`, `{{sender.name}}`) are resolved by a pure, unit-tested function (`src/lib/comms/templates.ts`) that reports which tokens didn't resolve — a template referencing an unknown token is rejected at save time; a send with an unresolved token (missing contact data, for example) is blocked rather than delivered with a literal `{{token}}`.
- **Composer**: a Send Email panel on the company detail page (`src/app/(dashboard)/companies/[id]/email/`) — pick a contact and/or a template (which prefills, not locks, subject/body), edit freely, send. The actual send (`src/lib/comms/send-email.ts`) is the one function every future send path (Phase C's scheduled/sequence sends) will share: it checks `Company.doNotContact`, resolves placeholders, validates every recipient and the subject against email-header injection (`src/lib/comms/validate.ts`), requires a `CONNECTED` mailbox, then calls the provider. A successful send updates the `EmailMessage` row to `SENT` and logs the existing free-text `EMAIL` activity (`src/lib/comms/activity-log.ts`) inside the same transaction; a failed send updates the row to `FAILED` and creates a `Notification` (type `DELIVERY_FAILURE`) for the sender instead of failing silently.
- **Notification model**: `Notification` (`userId`, `type`, `payload` JSON, `readAt`/`dismissedAt`) is the codebase's first general-purpose notification model — Module Five's `GeneratedReport.seenByIds` was deliberately report-specific by its own design note. It is not yet wired into the dashboard bell UI (that's a Phase E item, alongside unifying it with the report bell) — today it is written to but not yet displayed.
- **HTML body handling without a new dependency**: templates/composer store and edit plain text (no rich-text/WYSIWYG editor exists in this phase), so `src/lib/comms/sanitize-html.ts` escapes HTML-significant characters and converts newlines to `<br>` rather than pulling in an HTML-parsing sanitizer library. This was flagged in the approved plan as a dependency decision needing sign-off (`sanitize-html`); resolved by not needing rich-text authoring at all in Phase A, so the decision is deferred rather than made — revisit only if a future phase adds a real HTML editor.

## Features shipped (Phase B)

- **CASL-safe default-deny consent**: `Contact.emailPermitted` defaults `false` and `Contact.doNotContact` defaults `false` — a contact cannot receive a contact-linked send until a `ConsentRecord` establishes permission. `ConsentRecord` (`src/lib/comms/consent.ts`) is append-only, mirroring Module One's `HistoricalScoreRecord`/`Company.currentHistoricalScoreId` "full history + denormalized current state" pattern exactly. `recordConsent()` is the one place that pattern is written to, called by both the authenticated compliance UI (with `recordedById` set) and the public unsubscribe link (with `recordedById` left `null` — no user performed that action, the contact did).
- **`sendEmail()` gains a second, mandatory consent gate**: alongside Phase A's `Company.doNotContact` check, every send now also requires a linked contact with `Contact.emailPermitted && !Contact.doNotContact`, and the body must contain a resolved `{{unsubscribeLink}}` — enforced inside `sendEmail()` itself (`src/lib/comms/send-email.ts`), the one function every future send path shares, not left to the composer alone. `contactId` is a required field, not optional; there is no ad hoc/untracked-address send path.
- **Mandatory unsubscribe placeholder**: `{{unsubscribeLink}}` joins the known-placeholder list (`src/lib/comms/templates.ts`) and a template cannot be saved without referencing it (`src/app/(dashboard)/settings/email-templates/actions.ts`) — CAN-SPAM requires a working unsubscribe mechanism in every commercial email. The template editor's default body scaffold already includes it.
- **Self-verifying unsubscribe tokens** (`src/lib/comms/unsubscribe-token.ts`): HMAC-SHA256-signed, base64url-encoded `contactId + expiry` (400-day expiry — CAN-SPAM only requires 30 days of continued function, this is generous headroom), verified with a constant-time comparison and no database lookup needed to confirm authenticity. `UNSUBSCRIBE_TOKEN_SECRET` follows the exact optional-in-dev/required-in-production pattern as `TOKEN_ENCRYPTION_KEY`.
- **Public, no-login `/unsubscribe` page**: one click, no account needed (CAN-SPAM: "no fees or extra steps"), idempotent (following the same link twice never double-records a `WITHDRAWN` consent entry), rate-limited per IP (`checkRateLimit`, reusing the existing Postgres-backed limiter). `src/proxy.ts` gained a `PUBLIC_ALWAYS_ROUTES` category, distinct from `/login`'s `PUBLIC_ROUTES` — an *authenticated* visitor must still see this page rather than being bounced to `/dashboard`.
- **Communication Compliance page** (`/settings/communication-compliance`, `manage_communication_compliance`): search contacts by name/email/company, view each one's current status and full consent history, and record a new `EXPRESS`/`IMPLIED`/`WITHDRAWN` entry. Also reachable directly from a contact on its company page (a status badge plus a "Manage consent" link pre-filtered to that contact), per the plan's "also reachable from the contact itself" requirement.
- **Sender mailing address**: `WorkspaceSettings.mailingAddress` (a new field on the existing Module Four singleton-row settings table, not a new table) resolves the `{{sender.mailingAddress}}` placeholder — CAN-SPAM requires a valid physical mailing address in every commercial email.

## Features shipped (Phase C)

- **`sendEmail()` refactored around a shared `prepareSend()`/`attemptDelivery()` split** (`src/lib/comms/send-email.ts`) so the composer's immediate send, a scheduled send, and a sequence step's send all share the exact same consent gate, placeholder resolution, and provider-call/finalize logic — no third copy that could drift from the other two. `prepareSend()` re-runs in full every time a send actually happens, never trusted from an earlier check (schedule time or enrollment time), since consent/connection state can change in between.
- **Scheduled sends**: the composer gained an optional "Send at" field (`schedule_email` permission) — `scheduleEmail()` validates immediately for fast feedback but stores the **raw, unresolved** subject/body with `status: SCHEDULED`; placeholders (including `{{unsubscribeLink}}`) are re-resolved fresh at actual send time, not frozen at schedule time. A new worker tick (`send-scheduled-email-tick`, every 5 minutes — shorter than `reports-tick`'s hourly cadence, since "send at 2pm" carries a real timeliness expectation reports don't) finds due rows and enqueues one durable send job per row (`send-scheduled-email`, singleton-keyed on the message id). A scheduled send whose contact revoked consent (or whose mailbox got disconnected) in the meantime is marked `FAILED` with that reason and a `SCHEDULED_EMAIL_FAILED` notification — never silently sent anyway. The person who scheduled it can cancel it any time before it fires (`cancelScheduledEmail`, ownership-checked).
- **Follow-up sequences** (`/settings/sequences`, `manage_sequences` to design; `enroll_in_sequences` to enroll): table-driven, admin-authored step lists — `WAIT` (days), `EMAIL` (a **shared** template only — a personal template is one salesperson's own voice, not meant for an automated multi-user campaign), and four TASK-like types (`TASK`/`CALL_REMINDER`/`DEMO_REMINDER`/`TRIAL_FOLLOWUP`) that create a real `Task` row via the existing follow-up infrastructure rather than a parallel one. `src/lib/comms/sequences.ts` is the pure core: `nextActionableStep()`/`firstActionableStep()` fold `WAIT` steps' days into the due-date offset for the next actionable step (a `WAIT` step has no side effect of its own — it only delays), and `previewSteps()` renders every step (including waits) with its cumulative day offset for the enrollment confirmation screen, per the plan's "show every step before enrolling" requirement.
- **Explicit enrollment only**: `enrollInSequence()` is the one path a `SequenceEnrollment` is ever created — from a company's detail page, after previewing every step. Requires a contact when the sequence contains an `EMAIL` step (no contact, no consent to check); blocks a second concurrent enrollment of the same company in the same sequence.
- **Durable execution via `sequence-tick`** (every 5 minutes, same idiom as `reports-tick`): finds every `ACTIVE` enrollment whose `nextStepDueAt` has passed and enqueues one `run-sequence-step` job per row (singleton-keyed on `enrollmentId:stepId`). `processDueSequenceStep()` re-checks stop conditions **fresh, before running anything**: a contact who opted out stops the enrollment (`STOPPED_OPT_OUT`); a company that reached the sequence's stop stage stops it (`STOPPED_STAGE`) — both send a notification and never run the step. `SequenceStepRun`'s `@@unique([enrollmentId, stepId])` is the duplicate-send-prevention backstop, the same role Module Five's `singletonKey` dedup plays for scheduled reports — a step can be offered by overlapping ticks without ever executing twice.
- **No uncontrolled loops**: a sequence is a finite, admin-authored step list, never generated or recursive; the tick advances past exactly one actionable step per successful run (folding any intervening `WAIT` steps), so a sequence is structurally incapable of looping and terminates in at most `steps.length` ticks' worth of processing.
- **Manual pause/resume/cancel** are simple `SequenceEnrollment.status` transitions (`pauseEnrollment`/`resumeEnrollment`/`cancelEnrollment`), exposed on the company detail page's new Follow-up Sequences panel alongside the enrollment's current step progress ("Step 2 of 4") and next-due time.
- **Steps are locked once a sequence has any enrollment** (active or historical) — `assertStepsEditable()` in the sequence-builder actions blocks adding/reordering/removing a step the moment one enrollment exists, so an in-flight enrollment's `currentStepOrder` pointer can never be invalidated out from under it. Deactivate and create a new sequence instead of editing a used one.
- **Sequences are deactivated, never hard-deleted** — there is deliberately no delete action. `FollowUpSequence`'s cascade would otherwise destroy `SequenceEnrollment` history (including completed/stopped enrollments) the moment a sequence row was removed, which would erase exactly the audit trail the plan asks every step run to preserve.

## Post-Phase-C review and fixes

Before starting Phase D, the user asked for a review of Phases A–C and concrete recommendations. Reading the actual code (not relying on memory of what was built) surfaced four real gaps, all fixed in this same checkpoint:

- **The dashboard bell showed nothing from `Notification`.** Every `DELIVERY_FAILURE`/`SCHEDULED_EMAIL_FAILED`/`SEQUENCE_COMPLETED`/`SEQUENCE_PAUSED` row created since Phase A had zero UI surface — `dashboard-shell.tsx` only ever read `GeneratedReport`. Fixed with a minimal, additive change rather than pulling forward Phase E's full unification: `src/lib/notifications.ts`'s `describeNotification()` renders one human-readable line per `NotificationType`; the dashboard layout now also queries unread `Notification` rows and passes them to a generalized `NotificationBell` with its own "Notifications" section, mark-one-read and mark-all-read actions (`src/app/(dashboard)/notifications-actions.ts`, ownership-scoped in the `where` clause exactly like `markGeneratedReportSeen`). `GeneratedReport` and `Notification` remain two separate models under the hood — true unification is still a Phase E item — this just makes the already-written rows visible.
- **Email-send rate limiting was one bucket per provider, shared by every user.** `callEmailProvider()` keyed its rate limit on `providerName` alone (`email-send:microsoft`), not per connection as the original Phase A plan specified (`email-send:<providerConnectionId>`) — one user's active sequence or scheduled sends could throttle every other user connected to the same provider. Fixed by adding `connectionId` to `ConnectedAccount` (populated by `getUsableAccessToken()`) and an optional `connectionId` on `EmailProviderCallOptions`, used by both providers' actual send calls (token exchange/refresh calls, which are infrequent and don't need per-connection scoping, are unchanged). New test (`tests/integration/comms-provider-rate-limit.test.ts`) confirms two connections on the same provider have independent buckets.
- **A deactivated shared template kept being sent by an already-running sequence.** `processDueSequenceStep()` checked that a step's template still *existed* but not that it was still `active` — deactivating a shared template (rather than deleting it) had no effect on a sequence already using it. Fixed with one added check, recorded as a `FAILED` step run with a clear reason, same as every other step failure.
- **A sequence enrollment's panel never showed a past step's failure.** It showed "Step X of Y" and a stop reason, but a failed `SequenceStepRun` was only visible via the (previously invisible) notification or a direct database look. Fixed by having the company detail page fetch each enrollment's most recent `FAILED` step run (Prisma's nested `take` applies per parent row, not globally) and rendering it inline in `SequenceEnrollmentPanel`.

None of these required new schema or new architecture — all four were small, contained fixes layered onto Phase C's existing design, confirmed via a fresh read of the merged code rather than assumed correct from memory.

## Features shipped (Phase D1)

- **`EmailProvider` interface gains three calendar methods**: `createCalendarEvent`/`updateCalendarEvent`/`cancelCalendarEvent`, implemented in `MockEmailProvider` (deterministic fake ids, plus a `SIMULATED_CALENDAR_FAILURE_TITLE` sentinel mirroring `SIMULATED_SEND_FAILURE_ADDRESS` — the only way tests exercise the `ERROR` path), `MicrosoftGraphProvider`, and `GoogleProvider`. No separate `CalendarProvider` interface — both real providers already cover mail and calendar through the exact same OAuth connection (Phase A's `ProviderConnection` design anticipated this), so extending the one interface avoided a parallel type/factory system for no real benefit.
- **Timezone handling is provider-specific, not a shared abstraction**: Microsoft Graph's event API requires a bare wall-clock string ("2026-08-01T14:00:00", no offset) plus a separate `timeZone` field telling Graph how to interpret it — `src/lib/comms/calendar-time.ts`'s `formatWallClock()` handles this (unit-tested across a DST-crossing case and a date-boundary case). Google's Calendar API is more forgiving and accepts a full RFC3339 instant (`toISOString()`) directly alongside `timeZone` for display purposes only — no conversion helper needed there.
- **Calendar API calls get their own rate-limit bucket**, not a slice of the same connection's email-send budget: `EmailProviderCallOptions` gained an optional `bucketPrefix` (defaults `"email-send"`, calendar calls pass `"calendar"`) so a burst of appointment scheduling can never eat into the quota a sequence/scheduled-send needs, and vice versa — directly informed by the post-Phase-C rate-limiting fix above, applied proactively here rather than repeating the same gap.
- **`src/lib/comms/appointments.ts`** — `createAppointment()`/`updateAppointment()`/`cancelAppointment()`, mirroring `send-email.ts`'s "create the row first, attempt the provider call, finalize" pattern: an `Appointment` row always exists once creation is attempted, `providerEventId` stays null and `status` becomes `ERROR` (with `lastError`) if the provider call fails — never a silently dropped request, never a claimed success that didn't happen.
- **Update and cancel always act through the *original creator's* connection**, not the acting session user's — read from the stored `Appointment.providerConnectionId` row, because the real calendar event lives on whichever mailbox actually created it regardless of who clicks "reschedule"/"cancel" in the UI. Cancelling is idempotent (an already-`CANCELLED` appointment is a no-op success, and Google's 410 Gone on a re-delete is treated as success, not failure).
- **Appointment scheduling UI** on the company detail page (`manage_calendar_connections`, self-scoped to the caller's own company access): type (Demo/Trial Review/Follow-up), optional linked contact, title, start/end, timezone (free-text IANA name, validated against `Intl.DateTimeFormat` *before* ever reaching a provider call — an invalid zone is a clear form error, not an unhandled `RangeError` deep inside `createCalendarEvent`), and comma/newline-separated attendee emails. Attendee notifications are the provider's own (Graph's `/cancel` endpoint, Google's `sendUpdates=all` query param on create/update/delete) — this app never sends a competing notification email.

## Features shipped (Phase D2)

Scoped to **Microsoft Graph only**, confirmed with the user before writing any code: Gmail inbound push requires a Google Cloud Pub/Sub topic and service account provisioned outside this app (`users.watch()` + a push subscription on that topic), not just an OAuth client id/secret — real infrastructure a deploying team has to set up themselves in GCP Console, unlike everything else this module has needed so far. `GoogleProvider`'s five inbound methods throw a clear, deliberate "not implemented" error rather than silently no-op, so nothing upstream can mistake "not implemented" for "nothing to sync."

- **`EmailProvider` interface gains five inbound-sync methods**: `createInboundSubscription`/`renewInboundSubscription`/`cancelInboundSubscription`/`parseInboundWebhookPayload`/`fetchInboundMessage`. `parseInboundWebhookPayload` is deliberately pure (no network, no DB, no account) — safe to call before any authentication decision has been made about the request carrying it. `MockEmailProvider`'s versions are the only ones exercised by the full pipeline under test: since its `providerMessageId` is a self-describing base64url-encoded blob of the fixture content (a fresh `MockEmailProvider` instance is constructed per call — see `factory.ts` — so nothing can be held in memory between parse and fetch), a test posts a `buildMockWebhookBody([...])`-built payload through the real webhook route and gets real, deterministic content back out the other end, with no per-test mocking of the provider layer itself.
- **Webhook route** (`/api/comms/webhooks/[provider]`, no authentication — the provider itself is the caller): handles Graph's subscription-creation handshake (`?validationToken=` echoed back as plain text within its timeout window) and real notification delivery. Does only fast, local-DB work before responding 202 — Graph expects a response within a few seconds and retries aggressively otherwise — so the actual message fetch happens in the worker, not the route. Every per-notification failure (unknown subscription, wrong `clientState`, malformed payload, duplicate delivery) is a silent skip, never a distinct error response: the response body must never become an oracle an attacker can use to learn which subscription ids or secrets are valid.
- **Two-layer idempotency**, matching the plan's explicit requirement: a `WebhookEvent(provider, providerEventId)` unique constraint catches a redelivered *notification* (both Graph and Google document redelivery as expected, not a bug) before any provider API call is even made; `EmailMessage`'s own `(providerConnectionId, providerMessageId)` unique constraint separately catches a duplicate at the *message-content* level (a retried worker job after a partial failure, say). `eventId` is synthesized per notification as `${subscriptionId}:${resourceData.id}` — Graph's envelope carries no single canonical notification id of its own.
- **`clientState` verification, not just presence**: each `ProviderConnection` gets its own random secret (`generateClientState()`, `crypto.randomBytes(24)`) generated at subscribe time and stored on the connection row; Graph echoes it back on every notification, and the route rejects (silently) any notification whose value doesn't match what's actually stored for that `subscriptionId` — the receiver's proof a notification is genuine, not a forged POST to the public webhook URL guessing at valid ids.
- **Route validates and dedupes fast, the worker does the real work**: a verified, newly-recorded notification is enqueued as a `process-inbound-notification` job (singleton-keyed on `eventId`, defense in depth on top of the `WebhookEvent` constraint) rather than processed inline in the route — the same job-producer/job-consumer split this codebase already uses for scheduled sends and sequence steps, applied here for the first time to *inbound* traffic the app didn't itself initiate.
- **Contact matching is exact-or-nothing**: `matchContactForAddress()` looks up `Contact.email` case-insensitively and only returns a match when there is **exactly one** — zero matches (unknown sender) and more than one match (the same address reused across different companies' contacts, e.g. a shared front-desk inbox) are both treated identically as "unmatched," per the plan's explicit "never guess, never auto-create a company from an unknown sender" requirement extended to ambiguous matches too.
- **A matched reply stops the sequence immediately**, not on the next tick: `stopEnrollmentsForReply()` (`src/lib/comms/sequences.ts`) is called directly from the ingestion path the moment a match is confirmed, transitioning every `ACTIVE`/`PAUSED` enrollment for that contact to `STOPPED_REPLY` (finally reachable — the enum value has existed since Phase C) and sending one `NEW_REPLY` notification per enrollment stopped. When there's no active enrollment to stop, a general `NEW_REPLY` notification still goes to the company's assigned salesperson (falling back to the receiving mailbox's owner if the company is unassigned) — a reply is worth surfacing even outside a sequence.
- **`/settings/communications-review`** (`view_team_communications` — an existing Phase A permission that previously gated no real UI, now finally wired up): every unmatched inbound message (`contactId: null`, `reviewedAt: null`), oldest first, with sender/subject/body preview. Two actions: link to a contact by exact email (re-running the same `stopEnrollmentsForReply()` a successful automatic match would have, so a manually-resolved reply still stops an in-flight sequence), or dismiss without linking (spam, wrong number, genuinely unrelated) — both stamp `reviewedAt`/`reviewedById` so the item drops out of the queue either way.
- **Subscription lifecycle via an hourly `inbound-subscription-tick`**: for every `CONNECTED` connection, creates a subscription if none exists, renews one within 24 hours of its ~2.9-day Graph-imposed expiry (`SUBSCRIPTION_LIFETIME_MINUTES = 4200`, comfortably under Graph's documented ~4230-minute ceiling), and leaves a fresh one alone. Runs the work directly in the tick (unlike `reports-tick`/`send-scheduled-email-tick`, which enqueue a job per row for a separate consumer) — it's already in the worker process, and a per-connection create/renew call is one external round trip, not something that benefits from its own job-level retry semantics. One connection's failure (an unconfigured Google connection, a revoked token) is caught and logged per-connection, never blocking any other connection's tick.

## Features shipped (Phase E)

Two independently-scoped pieces, both confirmed with the user via explicit choices before writing any code (see "Deviations" below for the full reasoning):

- **Bounce detection via inbound sync, not a dedicated delivery-status webhook.** Neither Microsoft Graph nor Gmail expose a webhook for "was this message delivered" or "did this message bounce" when sending from a regular delegated mailbox — that's a transactional-email-service-only feature (SES/SendGrid/Postmark actually relay the mail themselves and get real DSN callbacks; Graph/Gmail here are the user's own mailbox, and neither platform's docs describe any equivalent for personal/business mail). A bounce instead arrives as an ordinary inbound email — a Non-Delivery Report from the mail system itself. `isBounceMessage()` (`src/lib/comms/inbound-sync.ts`) is a best-effort heuristic — sender address matching `postmaster@`/`mailer-daemon@`, or a subject line matching common NDR phrasing ("Undeliverable:", "Delivery has failed", etc.) — not a MIME-structure parse of a real RFC 3464 report (unreachable through the simple `$select=from,subject,body` fetch this module already uses; documented as a known limitation, not silently assumed reliable). `linkBounceToOriginalSend()` matches the bounce to the sent message it's bouncing via `providerThreadId` (Graph's `conversationId` — a genuine NDR lands in the same conversation as the message that triggered it) plus the same `providerConnectionId`, picks the most recent still-`SENT` message in that thread if more than one exists, flips it to `BOUNCED`, and notifies whoever sent it (`EMAIL_BOUNCED`, a new notification type — deliberately distinct from `DELIVERY_FAILURE`, which means "the provider call itself never succeeded," a materially different signal from "it was accepted, then failed downstream"). A recognized bounce never creates its own inbound `EmailMessage` row (it's mail-system noise, not a lead reply) and is routed around the normal contact-matching/review-queue path entirely.
- **Notification storage unification.** Through Phase D2, the dashboard bell read from two separate models: `Notification` (general events) and `GeneratedReport.seenByIds` (report-ready/failed events, tracked as a per-row array of user ids who'd seen it). Phase E removes `seenByIds` entirely and replaces it with the same mechanism every other event type in this app already used: one `Notification` row per recipient (type `REPORT_GENERATED`, payload `{ generatedReportId, reportKey, name, status }`), created by `worker/handlers/generate-report.ts` at the same time as the `GeneratedReport` row, in the same transaction. This is a genuine fan-out, not a 1:1 mapping — `ScheduledReport.recipientIds` can name several people, each with independent read state, which is exactly what per-user `Notification` rows give for free. `GeneratedReport` itself is unaffected (still owns report data, still the "download again" source, still access-controlled by `recipientIds`) — it just no longer separately tracks who's seen it. The bell (`NotificationBell`) collapsed from two sections ("Notifications" + "Report notifications") to one, reading only `Notification`; a `REPORT_GENERATED`/`SUCCEEDED` notification renders a "Download" link (computed server-side in `layout.tsx` from the payload) instead of a plain "Dismiss," preserving the exact download-and-mark-read UX the old report section had. The download route (`api/reports/generated/[id]/download`) now marks the matching `Notification` read via a JSON-path filter (`payload: { path: ["generatedReportId"], equals: id }`) instead of pushing onto `seenByIds`.

## Database additions

**Phase A** — migration `20260721140732_module_six_communications_phase_a` — purely additive, no drops:

- **`ProviderConnection`** — one per user (`@unique` on `userId`), encrypted tokens, `status` (CONNECTED/EXPIRED/REVOKED/ERROR), `scopes`, `tokenExpiresAt`, `lastError`.
- **`EmailTemplate`** — `visibility` (PERSONAL/SHARED), optional `ownerId`/`leadTypeId`/`pipelineStageId`, `language`, `active`.
- **`EmailMessage`** — `direction` (OUTBOUND/INBOUND, only OUTBOUND used so far), `toAddresses`/`ccAddresses`/`bccAddresses` (`String[]`), `status` (DRAFT/SCHEDULED/QUEUED/SENT/DELIVERED/FAILED/BOUNCED/CANCELLED/REPLIED — only DRAFT-adjacent/QUEUED/SENT/FAILED are reachable so far; the rest await later phases), `providerMessageId`/`providerThreadId`, optional `templateId`/`contactId`.
- **`Notification`** — `type` (9-value enum covering every event type across all phases, not just Phase A's `DELIVERY_FAILURE`), `payload` JSON, `readAt`/`dismissedAt`.

New indexes: `EmailMessage(companyId)`, `EmailMessage(contactId)`, `EmailMessage(status, scheduledFor)` (ready for Phase C's scheduled-send tick), `EmailTemplate(visibility, active)`, `EmailTemplate(ownerId)`, `ProviderConnection(status)`, `Notification(userId, readAt)`.

**Phase B** — migration `20260721151823_module_six_consent_compliance` — purely additive, no drops:

- **`Contact`** gains `emailPermitted Boolean @default(false)`, `doNotContact Boolean @default(false)`, `unsubscribedAt DateTime?`, `unsubscribeSource String?` — the denormalized "current state" half of the History/current-pointer split.
- **`ConsentRecord`** (new) — `contactId`, `type` (EXPRESS/IMPLIED/WITHDRAWN), `source`, `note?`, `occurredAt`, `recordedById?` (nullable — null for a self-service unsubscribe). Append-only; `@@index([contactId, occurredAt])`.
- **`WorkspaceSettings`** gains `mailingAddress String?`.

**Phase C** — migration `20260721175709_module_six_scheduling_sequences` — purely additive, no drops:

- **`FollowUpSequence`** (new) — `name`, `active`, `stopOnPipelineStageId?` (a **sequence-level**, not per-step, stop condition — see "Deviations" below), `createdById`.
- **`SequenceStep`** (new) — `sequenceId`, `stepOrder`, `type` (`WAIT`/`EMAIL`/`TASK`/`CALL_REMINDER`/`DEMO_REMINDER`/`TRIAL_FOLLOWUP`), `waitDays?`, `emailTemplateId?`, `taskTitle?`/`taskNotes?`. `@@unique([sequenceId, stepOrder])`.
- **`SequenceEnrollment`** (new) — `sequenceId`, `companyId`, `contactId?`, `status` (`ACTIVE`/`PAUSED`/`COMPLETED`/`CANCELLED`/`STOPPED_OPT_OUT`/`STOPPED_STAGE`/`STOPPED_REPLY`), `currentStepOrder`, `nextStepDueAt?`, `enrolledById`, `stoppedAt?`/`stopReason?`. `STOPPED_REPLY` exists for schema completeness across the whole plan but is unreachable until Phase D adds inbound reply detection.
- **`SequenceStepRun`** (new) — `enrollmentId`, `stepId`, `status` (`PENDING`/`SUCCEEDED`/`FAILED`/`SKIPPED`), `emailMessageId?` (`@unique` — a step run maps to at most one email), `errorMessage?`. `@@unique([enrollmentId, stepId])` is the duplicate-send-prevention constraint.

New indexes: `SequenceStep(sequenceId, stepOrder)` (unique), `SequenceEnrollment(status, nextStepDueAt)` (the tick's query), `SequenceStepRun(enrollmentId, stepId)` (unique), `SequenceStepRun(emailMessageId)` (unique).

**Phase D1** — migration `20260721200318_module_six_calendar_appointments` — purely additive, no drops:

- **`Appointment`** (new) — `companyId`, `contactId?`, `type` (`DEMO`/`TRIAL_REVIEW`/`FOLLOW_UP`), `title`, `startAt`/`endAt` (real UTC instants), `timezone` (IANA name), `attendeeEmails` (`String[]`), `providerConnectionId?` (nullable + `SetNull`, matching `EmailMessage`'s exact precedent — the appointment record outlives the connection it was created through), `providerEventId?` (null only when creation itself failed), `status` (`SCHEDULED`/`UPDATED`/`CANCELLED`/`ERROR`), `lastError?`, `createdById`.

New indexes: `Appointment(companyId)`, `Appointment(providerConnectionId)`.

**Phase D2** — migration `20260721210424_module_six_inbound_sync` — purely additive except one intentional loosening (below), no drops:

- **`EmailMessage.companyId` becomes nullable** (`String? `, `onDelete: SetNull` instead of `Cascade`) — the one non-purely-additive change in this migration. An inbound message from an unmatched sender genuinely has no company to attach to yet (the plan's "never auto-create a company from an unknown sender" requirement), so this had to loosen rather than stay required; every *outbound* row still always has a real `companyId` set at creation, unaffected in practice. `EmailMessage` also gains `fromAddress String?` (the raw inbound sender; always null for outbound, where the sender is implied by `providerConnection`), `reviewedAt`/`reviewedById` (the unmatched-review workflow), and a new `(providerConnectionId, providerMessageId)` unique constraint (message-content-level dedup — see "Features shipped" above). `EmailMessageStatus` gains `RECEIVED`, its own value for an inbound row rather than overloading `DELIVERED` (outbound-delivery-confirmed) or `REPLIED` (reserved, unused, for a future "this outbound message got a reply" display marker).
- **`ProviderConnection`** gains `inboundSubscriptionId`/`inboundSubscriptionExpiresAt`/`inboundClientState` — subscription bookkeeping plus the per-connection shared secret, all optional (only ever populated for a Microsoft connection in this phase).
- **`WebhookEvent`** (new) — `provider`, `providerEventId`, `receivedAt`. `@@unique([provider, providerEventId])` is the notification-level dedup constraint.

New indexes: `EmailMessage(direction, reviewedAt)` (the review-queue query), `EmailMessage(providerConnectionId, providerMessageId)` (unique).

Deliberately **not** in this migration (would forward-reference features that don't exist yet): `EmailMessage.bulkSendBatchId` (a later phase or Module Seven, if bulk send is ever built); any Gmail-specific Pub/Sub bookkeeping (no schema was added for a feature that isn't implemented — see "Features shipped" above).

Ambiguity note carried over verbatim from prior phases, still accurate: `matchContactForAddress()` treats more than one `Contact` sharing the same email (across different companies) the same as zero matches — both land unmatched for human review, never guessed.

**Phase E** — migration `20260722025437_module_six_phase_e_notifications` — the one migration in this module that isn't purely additive:

- **`NotificationType`** gains `EMAIL_BOUNCED` and `REPORT_GENERATED`.
- **`GeneratedReport.seenByIds` is dropped.** A real, intentional column removal, not an oversight — see "Features shipped" above for what replaces it (per-recipient `Notification` rows) and "Deviations" below for why this wasn't flagged as a concern before doing it: this is early-stage development data with no real users depending on unread-state continuity, and the column's entire purpose (bell read-tracking) is fully replaced by the new mechanism in the same migration, not left dangling.

No new indexes needed — `Notification(userId, readAt)` (Phase A) already covers the query pattern `REPORT_GENERATED` notifications use.

All six migrations applied to dev and test databases; `npx prisma generate` re-run after each. `prisma/seed.ts` re-run against dev after Phase A — **41 permissions seeded (up from 30)**; Phases B, C, D1, D2, and E added no new permission keys (D2 reuses `view_team_communications`, seeded in Phase A but previously gating no real UI; E needed no new permission at all — bounce detection and notification storage are both invisible plumbing, not a new gated feature).

## Packages added

None. Token encryption uses Node's built-in `crypto` (AES-256-GCM). HTML handling is a hand-rolled escape function, not `sanitize-html` (see "HTML body handling" above). OAuth/provider HTTP calls use the platform `fetch`, matching the AI-research provider pattern.

## Permission matrix

| Permission | Administrator | Manager | Salesperson |
|---|---|---|---|
| `connect_mailbox` | Yes | Yes | Yes |
| `send_email` | Yes | Yes | Yes |
| `schedule_email` | Yes | Yes | Yes |
| `manage_personal_templates` | Yes | Yes | Yes |
| `manage_shared_templates` | Yes | No | No |
| `manage_sequences` | Yes | No | No |
| `enroll_in_sequences` | Yes | Yes | Yes |
| `view_team_communications` | Yes | Yes | No |
| `manage_calendar_connections` | Yes | Yes (own) | Yes (own) |
| `manage_communication_compliance` | Yes | No | No |
| `send_bulk_email` | Yes | No | No |

Every permission in this matrix was seeded back in Phase A (per the full plan's matrix, added all at once rather than piecemeal per phase). As of Phase D2, every permission gates real UI except `send_bulk_email` (still awaiting a Phase-E-or-beyond bulk-send feature). `manage_calendar_connections` — despite its name suggesting only "connection management" — is what Phase D1 reuses to gate actually scheduling/updating/cancelling appointments too, since the plan's own matrix describes it as self-scoped ("own") for Manager/Salesperson exactly like the self-service actions it now covers. `view_team_communications` — seeded in Phase A, unused until now — is what Phase D2 reuses to gate `/settings/communications-review`; both reuses needed no new permission key.

`manage_personal_templates`/`manage_shared_templates` enforcement is ownership-aware, not just key-based: a Manager/Salesperson with `manage_personal_templates` can only edit/delete *their own* personal templates (verified by a rejected-edit test where a second user with the same permission is blocked from another user's template), while a user with `manage_shared_templates` (Administrator, by default) can also administratively edit/delete any personal template — covered in `tests/integration/email-templates.test.ts`.

## Consent/compliance controls (now live, Phase B)

Two consent checks apply to **every** send, with no exceptions: `Company.doNotContact` (Phase A) and `Contact.emailPermitted`/`doNotContact` plus a mandatory resolved unsubscribe link (Phase B) — both enforced inside `sendEmail()` itself, so no caller can accidentally skip either. The default is CASL-safe (opt-in): a contact cannot receive email until someone records `EXPRESS` or `IMPLIED` consent for them, which is also compliant under CAN-SPAM's opt-out model (a superset requirement satisfies both regimes without the app needing to guess which one applies to a given address). **This is a compliance-support system, not legal advice** — stated on the compliance page itself.

**A linked contact is now mandatory for every send** — `SendEmailParams.contactId` is a required field, not optional. There is no ad hoc/untracked-address send path: the composer requires picking an existing contact (the "To" field was removed entirely — the recipient address is always the contact's own recorded email, resolved server-side, never taken from client input), and `sendEmail()` rejects a missing contact, a contact with no email on file, and of course a not-yet-permitted or opted-out contact. This closes the gap an earlier draft of this report flagged (an ad hoc address bypassing consent entirely) by removing the bypassable path rather than trying to extend consent-tracking to untracked addresses — a company with no contacts yet must have one added (the existing Contacts panel already supports this inline) before anyone can email it from the CRM.

## Pages and routes added

| Route | Purpose |
|---|---|
| `/settings/email-connections` | Connect/disconnect a mailbox, view connection status |
| `/settings/email-templates` | Template list (shared + your personal), create form |
| `/settings/email-templates/[id]/edit` | Edit a template you're authorized to edit |
| `/settings/communication-compliance` | Search contacts, view consent history, record new consent |
| `/unsubscribe` | Public, no-login one-click unsubscribe |
| `/api/comms/oauth/[provider]/authorize` | Redirects to the provider's OAuth consent screen |
| `/api/comms/oauth/[provider]/callback` | Verifies CSRF state, exchanges code for tokens, stores the connection |
| `/settings/sequences` | List/create sequences, toggle active |
| `/settings/sequences/[id]/edit` | Step builder (add/reorder/remove) — locked once the sequence has any enrollment |
| `/settings/communications-review` | Unmatched inbound messages — link to a contact by email, or dismiss |
| `/api/comms/webhooks/[provider]` | Receives Microsoft Graph (and, once implemented, Google) push notifications for new inbox mail — no session auth, verified by per-connection `clientState` instead |
| Company detail page | "Email" panel (compose/send/schedule + history); Contacts panel shows a consent badge + "Manage consent" link; "Follow-up Sequences" panel (enroll with step preview, pause/resume/cancel); "Appointments" panel (schedule/reschedule/cancel a demo/trial-review/follow-up) |
| Settings hub (`/settings`) | Five new cards: Email Connections, Email Templates, Communication Compliance, Follow-up Sequences, Communications Review |

No new top-level route in Phase D2 or E — appointments, communications review, and the webhook endpoint round out the route list; Phase E touched no routing at all (its two changes, bounce detection and notification storage, are both invisible plumbing behind existing pages).

## Tests

**559 tests passing (550 from the Phase D2 checkpoint, largely unchanged, + 9 net new/changed from Phase E)**:
- Every prior test file — unchanged and still green, except the two Phase E touches below
- `tests/unit/bounce-detection.test.ts` (4 tests, new) — `isBounceMessage()` recognizes a `postmaster@`/`mailer-daemon@` sender (case-insensitively) and common NDR subject phrasing, and does not flag an ordinary reply
- `tests/integration/inbound-sync.test.ts` (+3 tests, new describe block) — a recognized bounce flips the matching sent message (same `providerConnectionId` + `providerThreadId`) to `BOUNCED`, notifies its sender, and creates no inbound `EmailMessage` row; a recognized bounce with no matching sent message is logged and dropped, not guessed at; an ordinary reply is never mistaken for a bounce
- `tests/integration/scheduled-reports.test.ts` (net +2 tests — 1 old `markGeneratedReportSeen` test removed, replaced by 3 new) — generating a report successfully creates one `REPORT_GENERATED` notification per recipient (fan-out, not one shared row) with the right payload; a permanent generation failure creates a `FAILED`-status notification; only the owning recipient can mark their own notification read (via the now-general `markNotificationRead`, not a report-specific action). The existing "serves the exact frozen payload" download test was also updated in place to assert the matching `Notification` is marked read, replacing its old `seenByIds` assertion.

No automated test ever sends real email, creates a real calendar event, reaches a real OAuth endpoint, or receives a real webhook delivery — `MockEmailProvider` is structurally the only provider active under `NODE_ENV=test`, exercised through the real webhook route end to end via `buildMockWebhookBody()`. The mock provider's inbound fixture shape gained an optional `providerThreadId` field this phase specifically so bounce-linking tests could construct a fixture that matches an already-sent message's thread.

**A note on test-suite stability**: across all phases of this module, full-suite runs have intermittently seen one or two *different*, unrelated test files fail with a generic hook/test timeout rather than an assertion failure. This checkpoint's full run was clean on the first attempt — 559/559 passing, no flakiness observed — though the pattern has recurred often enough in every other checkpoint that it's not assumed gone for good, just not present this time.

## Build result

- `npx prisma format` / `npx prisma validate` — clean
- `npx tsc --noEmit` — clean, re-checked after every batch of changes
- `npx eslint .` — clean
- `npm test` — 559/559 passing (see the stability note above)
- `npx next build` — succeeds; no new routes this phase, every prior route (including Phase D1/D2's) still registered
- `npm run worker` — reached "database schema is up to date" and loaded every handler (including the modified `generate-report.ts`) cleanly before hitting the same pre-existing local port-8080 conflict noted in earlier phases (an already-running worker instance) — confirms no `server-only`-class import bug in any changed file, the same check run and re-confirmed clean at the end of every phase this module.

## Browser walkthrough

**Not performed as an interactive click-through** — no browser automation tool was available this session, same limitation as every prior checkpoint. In its place: `tsc`/lint/the full test suite/a production build all passed, and the worker process was started for real. Two manual checks are still owed, carried forward and combined from prior checkpoints: (1) actually provisioning a real Microsoft Graph subscription against a deployed `APP_URL`, sending a real test email, and confirming it lands correctly (both the auto-matched and unmatched-review paths, and — new this phase — a real bounce from a genuinely invalid address correctly flipping the sent message to `BOUNCED`); (2) a click-through of the unified notification bell, confirming a real scheduled-report generation shows up correctly with a working Download link and marks itself read on click, now that it's sharing the bell with every other notification type.

## Remaining limitations / known gaps

- **A company with no email-addressed contacts cannot be emailed from the CRM yet** — this is the intended consequence of requiring a linked contact for every send (see Phase B's "Consent/compliance controls" above), not a bug.
- **Gmail inbound sync is not implemented.** It requires a Google Cloud Pub/Sub topic and service account provisioned outside this app — real infrastructure a deploying team has to set up in GCP Console, confirmed with the user as out of scope for this phase. `GoogleProvider`'s five inbound methods throw a clear "not implemented" error; a Google-connected user's replies are never picked up until this is built.
- **No delivery-status tracking beyond QUEUED→SENT/FAILED (or SCHEDULED→SENT/FAILED/CANCELLED) for outbound mail.** Delivered/bounced statuses exist on the enum but nothing updates a message to them — that requires the provider delivery-status webhooks Phase E adds (distinct from Phase D2's *inbound-mail* webhooks, which are built).
- **`Notification` rows are surfaced in the dashboard bell (fixed post-Phase-C, unified onto one system in Phase E) but there is no appointment-specific notification yet** — `APPOINTMENT_ACCEPTED`/`APPOINTMENT_DECLINED` exist on `NotificationType` for schema completeness but nothing creates one; detecting an attendee's accept/decline response would need webhook infrastructure applied to calendar responses specifically, which Module Six scoped to *mail*, not calendar, inbound events.
- **No bulk-send UI.** `send_bulk_email` is seeded and `EmailMessage` has no batch-grouping column yet — a salesperson sends one company/contact at a time today, and a sequence enrolls one company at a time (no bulk-enroll-a-list-of-leads flow). Deliberately out of scope for the whole module, per the original plan's §12.
- **The composer sends to exactly one contact per email** (cc/bcc remain free text for internal recipients) — there's no multi-contact "To" picker for emailing several tracked contacts on the same company at once yet.
- **The Communication Compliance and Communications Review pages have no pagination** — both cap results at 50 with a "narrow this down"/"resolve some to see more" note rather than paging through more. Fine at this team's likely scale; would need real pagination at a much larger volume.
- **A failed `EMAIL` step inside a sequence doesn't stop the sequence** — confirmed with the user directly: a provider hiccup on one step shouldn't necessarily kill an otherwise-healthy multi-step campaign, so the enrollment still advances. It is never silent, though — the step is recorded as a `SequenceStepRun` `FAILED` row *and* a `DELIVERY_FAILURE` notification is sent to whoever enrolled the company.
- **A sequence's steps are permanently locked the moment it has any enrollment, with no unlock path** — correct for protecting in-flight enrollments, but means fixing a typo in an already-enrolled sequence's step requires creating a whole new sequence rather than patching the existing one.
- **The appointment timezone field is free text**, validated only against `Intl.DateTimeFormat` accepting it as a real zone — no autocomplete/picker of common zones, so a typo like "Amerca/Toronto" is caught (rejected) but there's no guided list to choose from.
- **The manual "link to contact" flow in Communications Review requires an exact email match** — same rule the automatic matcher uses, deliberately, so a human resolving an ambiguous case can't accidentally attach a message to the wrong contact by typo; there's no fuzzy name-based search, only an exact email lookup.
- **Gmail inbound sync is not implemented**, and by extension **Gmail bounce detection doesn't exist either** — Phase E's bounce detection rides entirely on Phase D2's Microsoft-only inbound-sync pipeline. A Google-connected user gets neither reply detection nor bounce detection until Gmail inbound sync is built.
- **Bounce detection is a heuristic, not a guarantee.** `isBounceMessage()` matches on sender address and common subject phrasing, not a real MIME/RFC-3464 parse — a mail system using unusual bounce phrasing or a non-standard postmaster address could be missed (a false negative: a bounce quietly lands as an unmatched inbound message instead of flipping the sent message's status). There is also no "delivered" confirmation at all for either provider — this is a real, permanent capability gap in what Graph/Gmail expose for a delegated mailbox, not something a future phase can close without switching to a transactional email service.
- **No real Microsoft Graph subscription has ever been created against a live tenant.** Every automated test drives the real webhook route with `MockEmailProvider`-shaped payloads; the actual Graph API calls (`createInboundSubscription`, `renewInboundSubscription`, `fetchInboundMessage`) have only been reviewed against Graph's documented contracts, never exercised against a real Microsoft 365 tenant with a real, publicly reachable `APP_URL` — flagged below as the manual verification step still owed.

## Recommendations beyond Module Six

Every phase in the approved plan is now built. What's listed here is either explicitly out-of-scope work the plan itself deferred, or manual verification no automated build in this environment can substitute for — not an in-progress module:

1. **Gmail inbound sync**, if/when a Google Cloud Pub/Sub topic is actually provisioned — `GoogleProvider`'s five inbound methods are the exact seam to fill in; nothing else in `inbound-sync.ts`, the webhook route, the review UI, or bounce detection is Microsoft-specific, so Gmail bounce detection would come along with it essentially for free.
2. **A multi-contact "To" picker** for the composer, and/or a bulk-enroll flow for sequences, if real use shows a need to reach more than one tracked contact/company at a time (currently: one at a time for both) — the plan's §12 already flagged bulk send as a Module Seven candidate rather than in-scope here.
3. **Once real OAuth app registrations and a real deployed `APP_URL` exist** (Microsoft Entra / Google Cloud Console, not created this session per the plan's "never connect real accounts without approval"): a real manual connect/disconnect cycle, one real send to a controlled test mailbox, one real scheduled send, one real sequence enrollment, one real appointment create/update/cancel (confirming attendees actually receive Graph's/Google's real notifications), one real unsubscribe click-through, one real inbound reply landing correctly via a live Graph subscription (both the auto-matched and unmatched-review paths), and one real bounce (send to a genuinely invalid address and confirm the NDR correctly flips the sent message to `BOUNCED`) — before trusting Phases A–E against a live account. This is the single largest remaining gap: nothing in this module has ever touched a real Microsoft/Google endpoint.

## Deviations from the approved plan

**Phase A**: none in substance (see the `sanitize-html` dependency decision, resolved by not needing it this phase).

**Phase B**: the plan described the consent gate in terms of `Contact.emailPermitted`, which has no meaning for a send with no `Contact` row at all. An initial implementation scoped the gate to contact-linked sends only (skipping the check for an ad hoc address) — reviewed immediately after the Phase B checkpoint and revised to close that gap by making a linked contact **mandatory** for every send instead. `SendEmailParams.contactId` is required, the composer's free-text "To" field was removed, and the recipient is always resolved server-side from the contact's own record. Stricter than the plan explicitly called for, but defensible: a CRM whose purpose is tracking leads shouldn't have a send path that bypasses its own lead-tracking.

**Phase C**: two judgment calls the plan didn't fully pin down:
- **`SequenceStep.stopOnPipelineStageId` moved to the sequence level** (`FollowUpSequence.stopOnPipelineStageId`) instead of living on each individual step as the plan's schema sketch suggested. "Which stage ends this sequence" is one decision per sequence, not one an author should have to repeat identically on every step — a per-step field would only ever sensibly hold the same value on every step of a given sequence anyway.
- **No hard-delete for sequences** — the plan didn't explicitly address this, but `FollowUpSequence`'s natural cascade-delete behavior would destroy `SequenceEnrollment`/`SequenceStepRun` history the moment a sequence row was removed. Resolved by only ever exposing a `setSequenceActive()` deactivate toggle, never a delete action, matching the existing `RejectionReason`-style "deactivate, don't delete something with history" convention already used elsewhere in this codebase.

**Phase D1**: one scope decision beyond what the plan's Phase D sketch specified — **Phase D was split into D1 (calendar/appointments) and D2 (inbound sync)** at the user's explicit request, rather than built as one combined checkpoint the way the original plan's phase list implied. No functional deviation: everything the plan described for calendar/appointments (create/update/cancel, attendee invites, timezone handling, detecting provider errors) is built in D1; inbound sync is simply deferred to its own checkpoint, consistent with how every other phase in this module has already been scoped and shipped.

**Phase D2**: one scope narrowing, confirmed with the user via an explicit choice before writing any code — **inbound sync was built for Microsoft Graph only, not both providers as the plan's §9/§10 described**. The plan itself already flagged Gmail's push mechanism as fundamentally different (Pub/Sub, not a directly-configured webhook) but didn't anticipate that difference requiring real external infrastructure (a GCP project/topic) the deploying team must provision themselves — a prerequisite that doesn't exist for Microsoft. Rather than build against an infrastructure dependency that can't be verified in this environment, `GoogleProvider`'s inbound methods were built as clear, structurally-honest stubs (throwing "not implemented," never silently no-op) so a future pass has an unambiguous, well-defined seam to fill in rather than a half-built implementation nobody could test. Also, a small schema loosening beyond pure addition: `EmailMessage.companyId` had to become nullable (see "Database additions" above) — the plan's schema sketch didn't explicitly call this out, but it's a direct, necessary consequence of the plan's own "never auto-create a company from an unknown sender" requirement, which only makes sense if an unmatched message can exist without one.

**Phase E**: two deviations, both surfaced to the user via explicit choices before writing any code, exactly like Phase D2's provider-scoping decision:
- **"Delivery-status webhooks" doesn't map onto how Microsoft Graph or Gmail actually work for a delegated mailbox** — the plan's §12/§10 assumed a dedicated delivery-status webhook, modeled on how a transactional email service (SES/SendGrid) works. Verified against both providers' real documented capabilities (this session's own "verify before assuming" discipline) rather than building against an assumption: neither exposes one for regular mail sent from a personal/business mailbox, since neither is the actual relay — a bounce simply arrives as an ordinary inbound email. Resolved by building bounce detection as an extension of Phase D2's *existing* inbound-sync pipeline instead of a new webhook type, and explicitly documenting that no "delivered" confirmation is possible at all for either provider (a real, permanent gap, not a missed feature).
- **`GeneratedReport.seenByIds` is dropped, not merely deprecated** — the only column removal anywhere in this module (every other migration across all six phases was purely additive). The plan's own framing ("generalize storage, not just display, so `GeneratedReport` and `Notification` are eventually one system instead of two") already described exactly this outcome; dropping the column completes it in one step rather than leaving a redundant, unused column behind. Judged safe without a backfill: this is early-stage development data, and the column's one purpose (bell read-tracking) is fully replaced by per-recipient `Notification` rows created in the very same migration's accompanying code change — no notification-read history is lost, since none existed to preserve in a way that matters at this stage.
